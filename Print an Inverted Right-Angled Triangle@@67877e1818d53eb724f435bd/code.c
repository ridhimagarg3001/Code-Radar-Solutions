#include <stdio.h>

int main() {
    int N, i, r;
    scanf("%d", &N);
    for(r=a;r>=1;r--)
    {
        for(c=1;c<=r;c++)
        {
            printf("* ");
        }
        printf("\n");
    }
    return 0;
}