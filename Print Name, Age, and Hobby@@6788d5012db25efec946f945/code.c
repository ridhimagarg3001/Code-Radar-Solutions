#include <stdio.h>

int main() {
    char str[100], hobby[100];
    int age;
    scanf("%s", &str);
    scanf("%d", &age);
    sacnf("%s", &hobby);
    printf("Name: %s\nAge: %d\nHobby: %s", str,age,hobby);
    return 0;
}